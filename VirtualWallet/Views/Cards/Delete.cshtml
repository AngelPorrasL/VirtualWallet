@{
    ViewData["Title"] = "No";
}

@* <h2>Delete</h2>

<h3>Are you sure you want to delete this card?</h3>
<div>
    @foreach (var @item in @ViewBag.Cards)
  {
    <h4>Card Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Card Number
        </dt>
        <dd class="col-sm-10">
            @item.CardNumber
        </dd>

        <!-- Add more fields here if needed -->
    </dl>
  
    <form asp-controller="Cards" asp-action="Delete" asp-route-cardNumber="@item.CardNumber">
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>
  }
</div> *@

